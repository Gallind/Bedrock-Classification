"""python-pptx deck generator for the seabed-classification project.

Reuses the demo/shared/ cross-format layer (narration, palette, asset manifest)
shared with the Remotion video, but lays out its own slide deck — a different
order with a bit more detail than the ~5-minute video.
"""
